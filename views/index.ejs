<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>

<body>
    <div class="main">
        <div class="form">
            <form action="/create" method="post">
                <input type="text" name="title" id="input" placeholder="Write title here..." required>
                <textarea name="details" id="text" placeholder="Write tasks here..."></textarea required>
                <input type="submit" value="Create Task" id="btn">
            </form>
        </div>
        <div class="tasks">
            <% if(files.length>0) { %>
                <% files.forEach((file)=>{ %>
                    <div class="task">
                        <h3 class="title">
                            <%= file %>
                        </h3>
                        <a href="/files/<%= file %>" class="text">Read More..</a>
                        <a href="/edit/<%= file %>" id="edit">Edit</a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="noTaskText">No tasks Yet...</h3>
                            <% }%>
        </div>
    </div>
    <script src="/javascript/script.js"></script>
</body>

</html>